import Contact from '../components/Contact'

/** /contact — the contact section as a standalone page. */
export default function ContactPage() {
  return (
    <div className="pt-24 sm:pt-28">
      <Contact num="01" />
    </div>
  )
}
